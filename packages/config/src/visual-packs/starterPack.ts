import type { VisualPackConfig } from "@archetype-studio/core";

export const starterVisualPack: VisualPackConfig = {
  id: "starter-pack",
  label: "Starter Pack",
  supportedModes: ["mainstream"],
  assetSlots: ["hero", "accent", "cta"]
};
